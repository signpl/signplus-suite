# Signplus Suite Web MVP

현재 Windows 앱을 웹앱으로 이전하기 위한 첫 화면 프로토타입입니다.

## 실행

`webapp/index.html`을 브라우저에서 열거나, Node.js가 설치된 환경에서 간단한 정적 서버로 실행합니다.

```powershell
node webapp/server.js
```

브라우저에서 `http://localhost:4173`을 엽니다.
